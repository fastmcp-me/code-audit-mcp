// Test file with auto-fixable formatting issues only
export function simpleFunction(param1: string, param2: number) {
  // Bad spacing and formatting
  const result = param1 + String(param2);
  return result;
}

// Missing spaces around operators
export const calculation = (a: number, b: number) => a + b * 2;

// Inconsistent quotes and spacing
export const message = 'Hello world';

// Missing trailing semicolons
export const number = 42;
export const text = 'test';
